[project]
name = "prompt-craft"
version = "0.1.0"
description = "Automated Prompt Quality Metrics"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
